services:
  debrief:
    build: .
    image: debrief
    container_name: debrief
    restart: unless-stopped
    ports:
      - "3099:3099"
    volumes:
      - ~/.claude:/data/.claude:ro
    environment:
      - CLAUDE_DIR=/data/.claude
      - PORT=3099
